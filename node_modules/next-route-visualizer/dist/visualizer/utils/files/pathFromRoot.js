"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.pathFromRoot = void 0;
var _isType = require("./isType");
/**
 * Determines if the path is a route group
 * @param path 
 * @returns 
 */
var pathFromRoot = function pathFromRoot(path) {
  var pathSplit = path.split('/');
  var rootIndex = pathSplit.findIndex(function (element) {
    return element === 'app';
  });
  // find all route groups and remove them from the path
  var pathNoGroups = pathSplit.filter(function (element) {
    return !(0, _isType.isRouteGroup)(element);
  });
  return pathNoGroups.slice(rootIndex + 1).join('/');
};
exports.pathFromRoot = pathFromRoot;