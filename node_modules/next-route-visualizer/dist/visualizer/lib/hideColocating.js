"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hideColocating = void 0;
var _utils = require("../utils");
/**
 * hide collocation by removing the route subtrees that don't have a next file
 * @param route 
 * @returns 
 */
var hideColocating = function hideColocating(route) {
  // if no children, return route
  if (!route.children || route.children.length === 0) {
    return route;
  }

  //check if children have page or route files
  for (var i = 0; i < route.children.length; i++) {
    var child = route.children[i];
    if (child.data.type === 'Colocating') {
      route.children.splice(i, 1);
      i--;
    }
  }
  route = (0, _utils.setRouteSpans)(route);
  return route;
};
exports.hideColocating = hideColocating;