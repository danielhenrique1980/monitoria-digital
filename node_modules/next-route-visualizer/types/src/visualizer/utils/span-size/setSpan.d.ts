import { Route } from "../../types";
/**
 * Set the spans for the route and subroutes.
 * This is useful to calculate the x position of the nodes later
 * @param route
 * @returns Route
 */
export declare const setRouteSpans: (route: Route) => Route;
