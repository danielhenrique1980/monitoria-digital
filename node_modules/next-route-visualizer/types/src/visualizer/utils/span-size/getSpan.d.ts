import { Route } from "../../types";
/**
 * Get the span for all the children of the route.
 * Handles the groups of children too
 * @param routes
 * @returns
 */
export declare const getSpan: (routes: Route[]) => {
    maxSpan: number;
    spanning: number;
};
/**
 * calculate the span size from the max span
 * @param span
 * @returns
 */
export declare const getSpanSize: (span: number) => number;
