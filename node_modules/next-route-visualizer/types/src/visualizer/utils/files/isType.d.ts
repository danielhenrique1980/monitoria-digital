import { Route } from "../../types";
/**
 * Determines if the path is a route group
 * @param path
 * @returns
 */
export declare const isRouteGroup: (path: string) => boolean;
/**
 * Determines if the path is a route segment
 * @param path
 * @returns
 */
export declare const isRouteSegment: (path: string) => boolean;
/**
 * check if a route is colocating and set the type to Colocating
 * @param route
 * @returns
 */
export declare const isColocating: (route: Route) => Route;
