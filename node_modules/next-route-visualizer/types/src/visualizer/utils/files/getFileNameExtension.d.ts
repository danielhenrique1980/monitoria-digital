/**
 * get the file name and extension from a file name
 * @param fileName
 * @returns -> { name: string, extension: string}
 */
export declare const getFileNameExtension: (fileName: string) => {
    name: string;
    extension: string;
};
