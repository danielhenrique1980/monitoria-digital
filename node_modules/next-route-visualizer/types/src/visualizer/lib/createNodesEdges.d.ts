import { Route } from "../types";
import { Node, Edge } from "reactflow";
/**
 * Create the nodes and edges for the react flow component
 * @param route
 * @returns
 */
export declare const createNodesEdges: (route: Route) => {
    nodes: Node<any, string>[];
    edges: Edge<any>[];
};
